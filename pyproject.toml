[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "riffusion_api"
version = "0.1.0"

dependencies = [
    "pydantic>=2.0.0",
    "requests>=2.25.1",
    "streamlit>=1.34.0"
]
