[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "riffusion_api"
version = "0.1.0"

dependencies = [
    "requests>=2.25.1",
    "pydantic>=2.0.0",
    "streamlit>=1.34.0"
]
